name: Deploy to EC2
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to EC2 with environment variables
        env:
          PORT: ${{ secrets.PORT }}
          MONGO_DB_URL: ${{ secrets.MONGO_DB_URL }}
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@3.39.22.162 "
            echo '🚀 배포 시작' &&
            cd /home/ubuntu/workplus-back &&
            export PORT=${PORT} &&
            export MONGO_DB_URL=${MONGO_DB_URL} &&
            echo 'PORT=$PORT' &&
            git pull origin main &&
            export NVM_DIR=\"\$HOME/.nvm\" &&
            [ -s \"\$NVM_DIR/nvm.sh\" ] && . \"\$NVM_DIR/nvm.sh\" &&
            npm install &&
            pm2 restart workplus-back --update-env || pm2 start index.js --name workplus-back --update-env &&
            echo '✅ 배포 완료'
          "
